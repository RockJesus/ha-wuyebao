# 抓包指南：拿到物业宝的真实接口

本集成需要你提供三个接口信息，全部来自**你自己手机上的 App / 小程序**，操作大约 10 分钟。

---

## 你需要拿到的三样东西

| # | 操作 | 你要找的请求 | 记下什么 |
| --- | --- | --- | --- |
| 1 | 打开 App 登录 | 一个 **POST** 请求，请求体里能看到你的手机号（和密码/加密后的密码） | 服务器地址 + 登录路径 |
| 2 | 打开门禁/开门列表页 | 一个 **GET** 请求，返回里有你家几个门的名称和 ID | 设备列表接口路径 + 设备 ID/名称字段名 |
| 3 | 点一次「开门」 | 点门的瞬间发出的请求，参数里有门的 ID | 开门接口路径 + 参数名 + 请求方式 |

---

## 抓包工具选择

| 平台 | 推荐工具 | 说明 |
| --- | --- | --- |
| 安卓 | **Fiddler Classic**（电脑端）或 **Packet Capture / HTTPCanary**（手机端免 root） | 电脑端抓 HTTPS 需要在手机上装并信任证书 |
| 苹果 iOS | **Stream**（App Store 免费）或 Charles | 需要安装并信任描述文件证书 |
| 通用 | mitmproxy（命令行） | 适合熟悉命令行的人 |

**关键点**：手机和电脑必须在**同一个 Wi-Fi**；HTTPS 请求需要**信任抓包工具的根证书**，否则看到的都是加密乱码。

---

## 详细步骤（以 Fiddler + 安卓为例）

### 1. 电脑端准备
1. 下载安装 Fiddler Classic → 打开。
2. 菜单 Tools → Options → Connections：
   - 勾选 **Allow remote computers to connect**
   - 记住端口（默认 8888）。
3. 打开 HTTPS 解密：Tools → Options → HTTPS → 勾选 **Capture CONNECTs / Decrypt HTTPS traffic**。

### 2. 手机端准备
1. 连接同一个 Wi-Fi → Wi-Fi 设置里把**代理**设为手动：
   - 服务器：你电脑的局域网 IP（如 192.168.1.10）
   - 端口：8888
2. 手机浏览器访问 `http://<电脑IP>:8888` → 下载 Fiddler 根证书 → 安装。
3. 安卓：设置 → 安全 → 加密与凭据 → 安装证书 → CA 证书（不同系统路径略有差异）。
4. 手机确认浏览器能上网、Fiddler 里能看到流量，即成功。

### 3. 抓取三个请求
1. 打开物业宝 App → 退出登录 → 重新登录。
2. 在 Fiddler 里**按时间排序**，找到登录请求：
   - 特征：`POST`、路径含 `login`/`auth`/`token`/`signin` 等、请求体含手机号。
   - 记下：**完整 URL 的域名部分**（如 `https://api.jhcloud.example.com`，去掉路径）→ 这就是「API 服务器地址」。
   - 记下：域名后面的**路径**（如 `/api/login`）→ 登录接口路径。
3. 进入门禁/开门列表页 → 找到获取设备列表的请求：
   - 特征：`GET`，路径含 `device`/`door`/`list`/`bind` 等。
   - 点开响应（JSON），能看到数组，每项有个 ID 和名称。
   - 记下路径和里面 ID / 名称的**字段名**（如 `deviceId`、`name`）。
4. 点一次「开门」→ 找到那个请求：
   - 记下路径、请求方式（POST 还是 GET）、参数里的设备 ID 字段名。

### 4. 填进集成
- 「API 服务器地址」填第 2 步的域名。
- 「选项」里按记下的内容改三个路径、请求方式和字段名。
- 保存后集成会重新加载，出现按钮即成功。

---

## 常见坑

| 坑 | 怎么办 |
| --- | --- |
| 抓到但请求体是密文 | 登录接口对密码做了加密（如 RSA/AES）。此时**只要 token 能自动获取**就不影响使用；若登录本身就失败，把抓包结果发我 |
| 小程序抓不到明文 | 部分小程序走微信加密通道或云开发。建议改用 **App 版**抓包；没有 App 时换安卓 7 以下系统/模拟器，或使用「小程序反编译」工具（较复杂，可发我截图一起看） |
| 登录成功后列表里没门 | 可能是另一个「获取门禁」的接口，把 App 里每个页面都点一遍，观察响应里有门数据的请求 |
| 开门请求参数很复杂（带签名） | 优先用**重放 token + 原样参数**；如果开门参数依赖一次性签名，只能针对该平台定制，把抓包 JSON 发我 |
| 抓包工具装完手机无法上网 | 证书没信任，或代理端口没放行防火墙 |

---

## 安全提醒

- 抓包会看到你自己的账号流量，**只在自家电脑/网络进行**，抓完删除抓包文件。
- 密码、token 属于账号凭据，不要发到公开论坛；发给 AI 协助排查时建议打码手机号。
- 抓包期间建议把 App 里的免密支付、验证码短信等敏感操作暂停。

---

## 把抓包结果发我（推荐格式）

按下面格式发我，我可以帮你把集成改成**专用适配版**（不再需要手动填路径）：

```
服务器地址: https://...
登录: POST /api/xxx   请求体: {"phone": "...", "password": "..."}   响应里的token字段: xxx
设备列表: GET /api/xxx   响应数组字段: deviceList   每个门: {"deviceId":"...", "name":"..."}
开门: POST /api/xxx   参数: {"deviceId":"..."}   是否带 Authorization 头: Bearer/无
```
