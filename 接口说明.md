# 物业宝（家和云联）接口说明

本文档记录本集成所依赖的官方 API 契约，全部来自对官方 APK 的逆向与线上探测验证。
本集成直接调用这些接口，**无需抓包**。

---

## 1. 服务端

| 项 | 值 |
| --- | --- |
| API 根地址 | `https://wuye.jhws.top/` |
| 测试环境 | `https://test.jhws.top/`（未在本集成中使用） |
| 归属 | 深圳家和云联网络有限公司（JHCloud，官网 jhws.top） |
| 官方 App | 物业宝（业主），iOS App Store id1500178154；Android APK 由 `oss.jhws.top` 分发 |

## 2. 认证方式

所有请求都必须带请求头：

```
client_id: 984136053449428992
```

（`client_id` 是必须的：缺失时服务器返回 `请求头中无client_id信息!`。
该值提取自官方 App 二进制；H5 端使用 `614867715399749632`，测试多个已知
`client_id` 均能通过校验。可在集成「选项」中修改。）

需要登录态的接口（`grant` 段）再加：

```
Authorization: Bearer <accessToken>
```

## 3. 已验证接口

### 3.1 登录

```
POST /api/client/anon/token
Headers: client_id: <id>, Content-Type: application/json
Body:    {"username": "<手机号>", "password": "<密码>"}
```

成功：

```json
{ "code": 0, "data": { "accessToken": "...", "refreshToken": "...", ... } }
```

失败（HTTP 400）：

```json
{ "code": 1003, "data": "帐号或密码错误！" }
```

### 3.2 刷新 token

```
GET /api/client/anon/refresh_token?refreshToken=<refreshToken>
Headers: client_id: <id>
```

成功同样返回 `{"code":0,"data":{"accessToken":...,"refreshToken":...}}`。

### 3.3 门禁（大门）列表

```
GET /api/device/grant/gates?communityId=<小区ID>
Headers: client_id: <id>, Authorization: Bearer <accessToken>
```

**必须先选择小区**：不带小区参数时服务器返回
`{"code":-1,"data":"必须选择一个小区才能查询信息！"}`。
小区 ID 在配置时自动获取（见 3.5）；若服务器不识别 `communityId` 参数名，
本集成会自动改用 `communityCode` 重试（可在「选项」中固定参数名）。

成功返回门禁数组，记录包含 `id/gateId/deviceId`、名称、
`communityName/buildingName/unitName/floorName/roomName` 等地址字段、
`callNumber`（对讲号码）等。本集成按优先级自动识别 ID 与名称字段。

### 3.4 业主列表

```
GET /api/owner/grant/owners
Headers: client_id: <id>, Authorization: Bearer <accessToken>
```

返回当前账号的业主记录（姓名、手机号、小区等），用于「业主」传感器。

### 3.5 小区列表（配置时自动拉取）

```
GET /api/owner/anon/owners/community?phoneNumber=<手机号>
Headers: client_id: <id>
```

无需登录态，返回该手机号绑定的小区数组：

```json
{ "code": 0, "data": [
    { "phoneNumber": "...", "communityName": "金花围小区",
      "communityId": "1058409909948911616", "communityCode": 835, ... } ] }
```

配置向导用它在添加集成时让用户选择小区，并把 `communityId` 存入配置。

### 3.5 开门

App 内为 SIP 云对讲呼叫（见第 4 节），无公开 HTTP 路径常量。本集成默认按
最可能的 REST 风格调用，且路径/方式可配置：

```
POST /api/device/grant/gates/{gateId}/unlock
Headers: client_id: <id>, Authorization: Bearer <accessToken>
Body:    {}
```

`{gateId}` 自动替换为门禁 ID。若你的服务器支持其它路径，在集成「选项」中修改。

## 4. 其它已知接口（App 内使用，供扩展参考）

| 路径 | 用途 |
| --- | --- |
| `GET api/owner/anon/owners/community?phoneNumber=<手机号>` | 按手机号查所属小区（返回小区名、communityId 等） |
| `POST api/owner/anon/changepassword` | 修改密码 |
| `GET api/notice/grant/notices` | 社区公告 |
| `GET api/repair/repairs` | 报事报修 |
| `GET api/alarm/grant/alarms` | 报警记录 |
| `GET api/face/grant/faceinfo` / `api/face/mobile/grant` | 人脸信息 |
| `api/invitevisitor/grant` | 访客邀请（生成开门密码） |
| `api/push/v2/device/grant` | 推送设备注册 |
| `api/update/grant`、`api/app/grant/{md5}` | 应用更新 |
| SIP 对讲 | `new-sip.jhws.top:58583`（pjsua2 栈，账号密码为 `sip:login` / SIP token） |

## 5. 验证记录

- 登录接口：以 `client_id=984136053449428992` 请求，空凭据/错误凭据均返回
  `code=1003 帐号或密码错误！` → 确认该路径即登录入口、字段为 `username/password`。
- 刷新接口：`GET api/client/anon/refresh_token?refreshToken=xxx` 返回
  `Unauthorized grant type` → 确认路径、方法与参数名。
- 门禁接口：未带 token 访问返回 `401 {"code":1001,"data":"无效Token！"}` → 确认路径存在且需 Bearer；
  真实账号登录后不带小区参数访问返回 `{"code":-1,"data":"必须选择一个小区才能查询信息！"}` → 确认需要小区参数。
- 小区接口：`GET api/owner/anon/owners/community?phoneNumber=138...` 返回
  `{"code":0,"data":[{phoneNumber, communityName, communityId, communityCode, ...}]}`。
- 所有 `/api/` 前缀路径在未登录时统一返回 401（全局安全过滤器），无法通过 HTTP 枚举
  未公开的开门路径，这也是开门默认路径采用「可配置推测值」的原因。
